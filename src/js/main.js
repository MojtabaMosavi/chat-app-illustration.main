import Mobile from "./mobile";

/** 
 * Creting a name space to avoid naming confilict 
*/
const app = function(){
    document.addEventListener("DOMContentLoaded", () => {
        
    // initializing objects 
    const MobileApp = new Mobile();

    })
}()